name: hacs-validate

on:
  push:
  pull_request:
  workflow_dispatch:

permissions: {}

jobs:

  validate-hacs:
    runs-on: "ubuntu-latest"
    steps:
      -
        name: HACS validation
        uses: hacs/action@22.5.0
        with:
          category: integration
